type Props = {
  title: string;
  slug: string;
  isOpen: boolean;
  onClose: () => void;
};

const ShareDialog: React.FC<Props> = ({ title, isOpen, onClose }) => {
  return <></>;
};

export default ShareDialog;
